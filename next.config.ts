import type { NextConfig } from "next";

const nextConfig: NextConfig = {
 images: {
    domains: ['unsplash.com', 'istockphoto.com', 'freepik.com'], // Adicionando o domínio do Freepik
  },
};

export default nextConfig;
